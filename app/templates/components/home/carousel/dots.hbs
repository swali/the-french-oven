<ul class="home-carousel-dots">
  {{#each slides as |slide index|}}
    <li
      role="button"
      class="home-carousel-dots__dot {{if (eq index @activeSlide) "home-carousel-dots__dot--active"}}"
      onclick={{action @onDotClick index}}></li>
  {{/each}}
</ul>